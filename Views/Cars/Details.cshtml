@model CarsInsideGarage.Models.ViewModels.CarViewModel

@{
    ViewData["Title"] = "Car Details";
}
<h1>Car Details</h1>

<partial name="_CarForm" model="Model" />

<div class="mt-4">
    <a asp-action="Index" class="btn btn-primary btn-lg">Back to Garage Map</a>
</div>